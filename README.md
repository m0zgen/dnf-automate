# DNF Automatic Installer

Tested on CentOS / Fedora. This script wll install `dnf-automatic` functionality with parameters:

```
random_sleep = 300
apply_updates = yes
```

Just run `install.sh`:

```
./install.sh
```
